import { SuffixCurrencyPipe } from './suffix-currency.pipe';

describe('SuffixCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new SuffixCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
